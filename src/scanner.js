/**
 * 兼容导出：Kimi 扫描实现已迁入 src/scanners/kimi.js（变更 multi-tool-dimension）。
 * 既有测试与调用方继续从这里导入，断言保持不变作为迁移回归保护。
 */
export { listWireFiles, sha256File, scanWireFile, scanSessions } from './scanners/kimi.js';
